# QL-820NWB printer layer

The web release renders labels at 300 dpi and opens the operating-system print dialog. A future native wrapper can replace this adapter with Brother SDK calls without changing label templates.
